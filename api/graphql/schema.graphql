scalar GraphQLDate

type Comment {
  _id: ID!
  author: String!,
  comment: String!,
  createdOn: GraphQLDate!
}

type Post {
  _id: ID!
  author: String!,
  title: String!,
  body: String!,
  tags: [String!],
  createdOn: GraphQLDate!,
  updatedOn: GraphQLDate,
  comments: [Comment!]
}

input CommentInputs {
  author: String!,
  comment: String!
}

input AddPostInputs {
  author: String!,
  title: String!,
  body: String!,
  tags: [String!],
  createdOn: GraphQLDate!
}

type Query {
  postList(limit: Int): [Post!]!
  post(id: String!): Post!  
}

type Mutation {
  addComment(
    postId: String!
    comment: CommentInputs!
  ): Comment!
  addPost(issue: AddPostInputs!): Post!
}