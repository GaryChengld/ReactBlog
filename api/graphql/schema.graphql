scalar GraphQLDate

type Post {
  _id: ID!
  author: String!,
  title: String!,
  body: String!,
  tags: [String!],
  createdOn: GraphQLDate!,
  updatedOn: GraphQLDate
}

type Query {
  postList(limit: Int): [Post!]!
}